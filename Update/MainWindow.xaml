<Window x:Class="Update.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Update"
        mc:Ignorable="d"
        Title="AeroConnectorUpdater"
        WindowStyle="None" 
        WindowChrome.WindowChrome="{DynamicResource WindowChromeKey}" 
        Height="300" Width="500">
    <Window.Resources>
        <WindowChrome x:Key="WindowChromeKey" ResizeBorderThickness="0" CaptionHeight="50">
        </WindowChrome>
    </Window.Resources>
    <Grid Background="#292A35" >
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="155"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="20 0 20 10">
            <TextBlock Name="tb" HorizontalAlignment="Left" Foreground="White" VerticalAlignment="Top"/>
            <TextBlock Name="tb_percent" Text="wait..." Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" ></TextBlock>
            <ProgressBar HorizontalAlignment="Stretch" Background="#292A35" Height="10" Name="pbDown" Value="0" Maximum="2" VerticalAlignment="Top" />

        </StackPanel>
        </Grid>
</Window>
